<%= render partial: "shared/admin_navbar" %>

<body>
  <div class="row">
    <p class='col-12'>Invoices</p>
  </div>
  <% @invoices.each do |i| %>
    <li><%= link_to "Invoice ##{i.id}", admin_invoice_path(i) %></li>
  <% end %>
</body>
<%= render partial: 'navbar' %>

<%= render partial: 'form', locals: { method: :patch, url: merchant_bulk_discount_path(@merchant, @bd), class: 'merchant_bulk_discount_form', local: true, model: @bd} %>

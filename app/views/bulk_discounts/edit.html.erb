<%= form_with model: @bulk_discount, url: merchant_bulk_discount_path(@merchant, @bulk_discount), local: true do |f| %>
  <%= f.label :percentage %>
  <%= f.number_field :percentage %>

  <%= f.label :threshold %>
  <%= f.number_field :threshold %>

  <%= f.submit "Create Bulk Discount"%>
  <% end %>
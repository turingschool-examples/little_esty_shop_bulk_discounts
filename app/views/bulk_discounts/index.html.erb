<h1>Bulk Discounts</h1>
<% @discounts.each do |discount| %>
 <div id="discount-<%= discount.id %>">
    <ul>
      <li><%= "Discount: %#{(discount.percentage * 100).to_i} "%></li>
      <li><%= "Amount of items needed for discount: #{discount.quantity_threshhold}"%>
    </ul>
  </div>
<%end %>

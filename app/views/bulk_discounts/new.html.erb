<h1>New Discount Form</h1>

<%= render partial: 'form', locals: {
                                      path: merchant_bulk_discounts_path(@merchant),
                                      method: :post } %>

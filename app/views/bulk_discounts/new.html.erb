<%= form_with model: [@merchant, @bulk_discount], local: true do |f| %>
  <%= f.label 'percentage discount' %>
  <%= f.number_field :discount %>

  <%= f.label 'item threshold' %>
  <%= f.number_field :threshold %>

  <%= f.submit 'create'%>
<% end %>

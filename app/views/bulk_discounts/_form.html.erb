  <%= form_with model: model, url: url, class: 'new_discount_form', method: method, local: true do |form| %>
  <p><%= form.label :name %><br />
    <%= form.text_field :name %></p>

  <p><%= form.label :percentage %><br />
    <%= form.text_field :percentage %></p>

  <p><%= form.label :quantity_threshold %><br />
    <%= form.text_field :quantity_threshold %></p>

  <p><%= form.button "Submit"%></p>
  <% end %>
